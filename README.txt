Compile Bootstrap CSS, change @grid-float-breakpoint to @screen-desktop.

Add white-space:normal CSS rules to all tds with labels to keep them from messing with the td widths of the rest of the table if table is responsive.